ssh-keygen -t rsa -C "youremail@example.com" 创建ssh key
git ini 		初始化本地git仓库
git add<file> 	添加文件
git status		查看状态
git commit 		提交
git push		推送到仓库
git pull		从远程仓库拉取数据
git clone		从远程仓库拷贝数据

git rm --cached <file> 取消上传